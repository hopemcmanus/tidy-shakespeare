You are an expert R developer specializing in digital humanities, NLP pipelines, network analysis, and static HTML visualization generation.

Generate ONE single, production-ready R script that builds ALL interactive HTML pages for a Shakespeare corpus using the following PRE-EXISTING project structure (DO NOT CREATE OR MODIFY FOLDERS):

shakespeare/
├── data/
│   ├── raw/
│   ├── cleaned/
│   ├── processed/
│   │   ├── tokens/
│   │   └── filtered/
│   ├── json/
|   |   | full_text
│   │   ├── all_shakespeare.json
|   |   └── full_text/
│   │   |   └── *.json
│   ├── metadata/
│   │   ├── meta_shakespeare.csv
│   │   ├── meta_shakespeare.json
│   │   ├── tokenisation_log.csv
│   │   ├── tokenisation_log.json
│   │   ├── unique_characters.csv
│   │   ├── character_statistics.csv
│   │   └── characters_per_play.csv
│   ├── networks/
│   │   ├── network_metrics.csv
│   │   └── *.graphml
│   └── shakespeare.sqlite
├── plots/
│   ├── character_networks/*.png
│   ├── frequency/by_genre/*.png
│   ├── relationships/*.png
│   └── sentiment/*.png
└── interactive_networks/
    ├── characters/
    │   ├── index.html
    │   └── *.html
    └── correlations/
        ├── index.html
        └── *.html

────────────────────────────────
GLOBAL REQUIREMENTS
────────────────────────────────

1. The script MUST:
   - Assume ALL folders and files already exist
   - NEVER create new directories
   - NEVER overwrite data
   - Only write index.HTML file into current or create new folder plays with *.html

2. The script MUST:
   - Use ONLY static HTML generation via:
     • htmlwidgets
     • plotly
     • visNetwork OR networkD3
   - NO Shiny
   - NO flexdashboard
   - NO RMarkdown authoring

3. Allowed libraries only:
   - tidyverse
   - igraph
   - visNetwork OR networkD3
   - plotly OR heatmaply
   - htmlwidgets
   - DBI
   - RSQLite
   - jsonlite

4. The script MUST be:
   - Fully reproducible
   - With package auto-install checks
   - With clean logging messages for every output file
   - Fully commented
   - With a main() execution function at the bottom

────────────────────────────────
INPUT DATA SOURCES
────────────────────────────────

Use ALL of the following as inputs:

-  cleaned data
  /data/cleaned/*.csv


- Token & NLP data:
  /data/processed/tokens/
  /data/processed/filtered/

- Metadata:
  /data/metadata/meta_shakespeare.csv
  /data/metadata/meta_shakespeare.json
  /data/metadata/tokenisation_log.csv
  /data/metadata/tokenisation_log.json

- SQLite:
  /data/shakespeare.sqlite

- Pre-rendered plots (if exists):
  /plots/character_networks/*.png
  /plots/frequency/individual_plays/*_top_words.png
  /plots/relationships/correlations/section_correlations_*.png
  /plots/relationships/networks/bigram_networks_*.png
  /plots/sentiment/sentiment_*.png

────────────────────────────────
OUTPUT PAGES TO GENERATE
────────────────────────────────

A) PLAY INDEX HTML (one per play)
→ Save to:
/plays/*.html

────────────────────────────────
EXECUTION BEHAVIOR
────────────────────────────────

The script must:

- Safely iterate over:
  • All metadata rows
- Match play metadata via:
  • Gutenberg ID
  • OR short_title
- Validate:
  • Every play has a metadata row
- Gracefully skip with warning if data is missing
- Never crash mid-run

────────────────────────────────
FINAL OUTPUT FORMAT
────────────────────────────────

✅ OUTPUT ONLY ONE VALID R SCRIPT
✅ NO MARKDOWN
✅ NO EXPLANATORY TEXT
✅ NO PLACEHOLDERS
✅ PRODUCTION-READY
✅ DIRECTLY EXECUTABLE
✅ SINGLE PASS GENERATION
✅ NO INTERACTIVE USER INPUT

